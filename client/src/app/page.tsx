import Banner from "@/components/Banner";
import PromoteCard from "@/components/PromoteCard";

const Home = () => {
  return (
    <main>
      <Banner />
      <PromoteCard />
    </main>
  );
};

export default Home;
